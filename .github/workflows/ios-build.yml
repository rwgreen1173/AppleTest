name: Build and Deploy to Appetize

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-and-upload:
    runs-on: macos-14

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle Cache
        uses: gradle/actions/setup-gradle@v3

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      # 1. Compiles your Multiplatform project UI layout into an Apple simulator application bundle
      - name: Build App Simulator Bundle
        run: ./gradlew :composeApp:iosX64Binaries

      # 2. Automatically locates the newly generated .app bundle folder, packs it into a ZIP file, and copies it to the root workspace
      - name: Package Simulator App Bundle
        run: |
          echo "Locating your generated KMP application bundle..."
          APP_PATH=$(find composeApp/build -name "*.app" -type d | head -n 1)
          
          if [ -z "$APP_PATH" ]; then
            echo "ERROR: Could not locate compiled .app bundles inside composeApp/build outputs."
            exit 1
          fi
          
          echo "Found application target folder at: $APP_PATH"
          PARENT_DIR=$(dirname "$APP_PATH")
          APP_NAME=$(basename "$APP_PATH")
          
          cd "$PARENT_DIR"
          zip -r iosApp.zip "$APP_NAME"
          mv iosApp.zip $GITHUB_WORKSPACE/iosApp.zip
          echo "Successfully packed application binary structure."

      # 3. Securely pushes the finalized app zip file directly into your Appetize layout
      - name: Upload to Appetize.io
        run: |
          curl -X POST https://appetize.io \
               -H "X-API-KEY: ${{ secrets.APPETIZE_API_TOKEN }}" \
               -F "file=@iosApp.zip" \
               -F "platform=ios"
